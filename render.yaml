services:
  - type: static
    name: lecture-develop-001
    # env: static は不要なので削除してOK
    buildCommand: npm install && npm run build:render
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /main/*
        destination: /main/index.html
      - type: rewrite
        source: /math03/*
        destination: /math03/index.html